# Remote Claude Code via Telegram - Environment Configuration
# ===========================================================

# CLAUDE CODE AUTHENTICATION (Required - Choose ONE option)
# -----------------------------------------------------------

# Option 1: Claude Subscription OAuth Token (RECOMMENDED)
# --------------------------------------------------------
# Use your Claude Pro/Max subscription instead of paying for API usage
# Generate a long-lived token with: claude setup-token
# This will open a browser for authentication, then provide a token starting with "sk-ant-oat01-"
CLAUDE_CODE_OAUTH_TOKEN=your_claude_oauth_token_here

# Option 2: Anthropic API Key (Alternative)
# ------------------------------------------
# Use Console API key (incurs API usage charges)
# Get your API key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# GITHUB TOKEN (Required)
# -----------------------
# Used by GitHub CLI for authentication
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: repo, workflow (adjust based on your needs)
GH_TOKEN=your_github_personal_access_token_here

# TELEGRAM BOT TOKEN (Required)
# ------------------------------
# Get your bot token from @BotFather on Telegram:
#   1. Message @BotFather on Telegram
#   2. Use /newbot command to create a new bot
#   3. Follow instructions and copy the token provided
TELEGRAM_BOT_API_KEY=your_telegram_bot_token_here

# WORKSPACE PATH (Optional)
# --------------------------
# Path on your HOST machine to mount as the workspace
# Claude Code will create/edit files here, and they'll persist on your machine
# Default: ./workspace (creates a workspace folder in the current directory)
# WORKSPACE_PATH=/path/to/your/projects

# WORKING DIRECTORY (Optional)
# -----------------------------
# The directory INSIDE the container where Claude Code operates
# Should match the mount point in docker-compose.yml
# Default: /workspace
# WORKING_DIRECTORY=/workspace

# SENTRY MONITORING CONFIGURATION (Optional)
# -------------------------------------------
# Optional: Enable Sentry error tracking and performance monitoring
# Get your DSN from: https://sentry.io/
#   1. Create a Sentry account (if you don't have one)
#   2. Create a new project for your bot
#   3. Copy the DSN from project settings
# SENTRY_DSN=your_sentry_dsn_here
#
# Optional: Set environment name (default: production)
# Common values: development, staging, production
# SENTRY_ENVIRONMENT=production
